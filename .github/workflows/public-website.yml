name: public-website

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - 'public-site/**'
      - 'scripts/deploy-public-website.sh'
      - 'scripts/render-site-templates.py'
      - 'VERSION'
      - '.github/workflows/public-website.yml'
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Optional SemVer website version, for example 8.0.1'
        required: false
        type: string

permissions:
  contents: read

jobs:
  deploy-public-website:
    name: Deploy beagle-os.com website
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Resolve VERSION
        run: |
          BEAGLE_RELEASE_VERSION="${{ github.event.inputs.release_version }}" \
            bash scripts/resolve-release-version.sh --write VERSION

      - name: Setup SSH
        env:
          BEAGLE_PUBLIC_DEPLOY_SSH_KEY: ${{ secrets.BEAGLE_PUBLIC_DEPLOY_SSH_KEY }}
          BEAGLE_PUBLIC_DEPLOY_KNOWN_HOSTS: ${{ secrets.BEAGLE_PUBLIC_DEPLOY_KNOWN_HOSTS }}
        run: |
          install -d -m 0700 ~/.ssh
          printf '%s\n' "$BEAGLE_PUBLIC_DEPLOY_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 0600 ~/.ssh/id_ed25519
          printf '%s\n' "$BEAGLE_PUBLIC_DEPLOY_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 0644 ~/.ssh/known_hosts

      - name: Install rsync
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends rsync openssh-client

      - name: Publish website
        env:
          BEAGLE_PUBLIC_SITE_DIR: ${{ github.workspace }}/public-site
          BEAGLE_PUBLIC_SITE_TARGET: ${{ secrets.BEAGLE_PUBLIC_SITE_TARGET }}
        run: bash scripts/deploy-public-website.sh

      - name: Verify rendered website content
        run: |
          VERSION="$(tr -d ' \n\r' < VERSION)"
          RELEASE_TAG="v${VERSION}"
          GITHUB_RELEASE_URL="https://github.com/meinzeug/beagle-os/releases/tag/${RELEASE_TAG}"
          PUBLIC_UPDATE_BASE_URL="https://beagle-os.com/beagle-updates"
          render_dir="$(mktemp -d)"
          trap 'rm -rf "${render_dir}"' EXIT
          rsync -a --delete public-site/ "${render_dir}/"
          python3 scripts/render-site-templates.py \
            "${render_dir}" "${RELEASE_TAG}" "${GITHUB_RELEASE_URL}" "${PUBLIC_UPDATE_BASE_URL}"
          grep -q 'Beagle OS' "${render_dir}/index.html"
          grep -q "Release ${RELEASE_TAG}" "${render_dir}/index.html"
          grep -q "Current release: ${RELEASE_TAG}" "${render_dir}/download/index.html"
          grep -q "${RELEASE_TAG}" "${render_dir}/download/index.html"
          grep -qi 'beagleos-gaming.png' "${render_dir}/styles.css"

      - name: Verify live website content
        run: |
          VERSION="$(tr -d ' \n\r' < VERSION)"
          RELEASE_TAG="v${VERSION}"
          curl -fsSL https://beagle-os.com/ >/tmp/beagle-homepage.html
          curl -fsSL https://beagle-os.com/download/ >/tmp/beagle-download.html
          curl -fsSL https://beagle-os.com/styles.css >/tmp/beagle-styles.css
          curl -fsSI https://beagle-os.com/assets/img/beagleos-gaming.png >/tmp/beagle-wallpaper.headers
          grep -q 'Beagle OS' /tmp/beagle-homepage.html
          grep -q "Release ${RELEASE_TAG}" /tmp/beagle-homepage.html
          grep -q "Current release: ${RELEASE_TAG}" /tmp/beagle-download.html
          grep -q "${RELEASE_TAG}" /tmp/beagle-download.html
          grep -qi 'beagleos-gaming.png' /tmp/beagle-styles.css
          LEGACY_PROVIDER_TERM="Prox""mox optional"
          if grep -qi "${LEGACY_PROVIDER_TERM}" /tmp/beagle-homepage.html; then
            echo "Live website still contains legacy provider copy" >&2
            exit 1
          fi
